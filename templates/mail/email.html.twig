<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Importer Email Template</title>
		<style>
			body {
				font-family: Arial, sans-serif;
				margin: 0;
				padding: 0;
				background: url('https://via.placeholder.com/1920x1080') center/cover no-repeat;
			}

			.header {
				background: linear-gradient(to right, #007bff, #4c8bf5);
				padding: 20px;
			}

			.content {
				padding: 20px;
				background-color: #ffffff;
				border-radius: 10px;
				box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
			}

			h1 {
				font-size: 36px;
				color: #333333;
			}

			p {
				font-size: 16px;
				line-height: 1.6;
				color: #555555;
			}

			.content {
				transition: transform 0.3s ease-in-out;
			}

			.content:hover {
				transform: scale(1.02);
			}
		</style>
	</head>
	<body>
		<div class="header"></div>

		<div class="content">
			<h1>{{ pimcore_input("headline") }}</h1>
			<p>{{ pimcore_wysiwyg('message') }}</p>
		</div>
	</body>
</html>
